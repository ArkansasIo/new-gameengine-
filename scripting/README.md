# Scripting & Automation

This folder contains scripts for editor automation and batch operations.

## Example: Batch Rename Assets

Run `automation_example.py` to batch rename `.gd` files in the editor folder with a prefix.

```
python scripting/automation_example.py
```

You can add more scripts for asset import, export, or custom editor actions.
